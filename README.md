# CS3800RestfulEncryption
Fernet encryption

enter text into leftmost textbox, press encrypt button to encrypt data
decrypt button to convert cipher hexadecimal back into plaintext

hosted at https://restful-encryption-224601.appspot.com/
made using python 3

to run locally:
pip install -r requirements.txt
python main.py
navigate to localhost:8080
